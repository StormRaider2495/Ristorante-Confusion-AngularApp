// Be descriptive with titles here. The describe and it titles combined read like a sentence.

describe('Demo Test', function() {
    it('has a dummy spec to test 2 + 2', function() {
        y = 2 + 2;
        expect(y).toEqual(5 - 1);
    });
});
